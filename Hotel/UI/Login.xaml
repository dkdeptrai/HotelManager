<Window x:Class="Hotel.UI.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hotel.UI"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800">
    <Grid>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Label Grid.Column="0" Grid.Row="0" Content="LoginID" />
            <TextBox Name="TxtLoginID" Grid.Column="1" Grid.Row="0" Width="250" />
            <Label Grid.Column="0" Grid.Row="1" Content="Password" />
            <PasswordBox Name="TxtPassword" Grid.Column="1" Grid.Row="1" Width="250" />
            <Button Grid.Column="0" Grid.Row="2" Content="Login" Click="Button_Click" />
        </Grid>
    </Grid>
</Window>
