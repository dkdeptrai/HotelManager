<Window x:Class="Hotel.UI.DeleteRoom"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hotel.UI"
        mc:Ignorable="d"
        Title="DeleteRoom" Height="450" Width="800">
    <Grid Margin="15 5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Label Grid.Column="0" Grid.Row="0" Content="RoomNum"></Label>
            <TextBox Name="TxtRoomNum" Grid.Column="1" Grid.Row="0"></TextBox>
            <Button Grid.Column="0" Grid.Row="1" Content="Delete Room" Click="Button_Click"/>

        </Grid>
    </Grid>
</Window>
